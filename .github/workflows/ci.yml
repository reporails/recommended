name: CI

on:
  push:
    branches:
      # Release branches: run QA on every push (e.g. 0.1.0)
      - "[0-9]*.[0-9]*.[0-9]*"

jobs:
  qa:
    name: QA gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: recommended

      # TODO: Re-enable once reporails-cli >=0.4.0 is published on PyPI.
      # The runtime Dockerfile requires >=0.4.0 but only 0.3.0 exists.
      - name: Skip — waiting for reporails-cli 0.4.0
        run: echo "QA skipped — reporails-cli >=0.4.0 not yet published"
