rules:
  - id: AILS_CLAUDE_M1-body-too-long
    message: "Rule file body exceeds 40 lines"
    severity: WARNING
    languages: [generic]
    pattern-regex: "(?s)^---.*?---(.*)$"
    paths:
      include:
        - "{{rules_dir}}/**/*.md"

  - id: AILS_CLAUDE_M1-scope-too-broad
    message: "Rule file has broad paths scope"
    severity: WARNING
    languages: [generic]
    pattern-regex: "paths:\\s*\\n\\s*-.*\\*\\*"
    paths:
      include:
        - "{{rules_dir}}/**/*.md"
