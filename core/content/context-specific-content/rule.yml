rules:
  - id: RRAILS.C.0002.check.0001
    message: "Domain-specific content may belong in @imports"
    severity: WARNING
    languages: [generic]
    pattern-regex: "(?i)(endpoint|api route|database schema|query|mutation).*\\n.*\\n.*(endpoint|api route|database schema|query|mutation)"
    paths:
      include:
        - "{{instruction_files}}"
