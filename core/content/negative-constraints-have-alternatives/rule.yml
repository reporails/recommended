rules:
  - id: RRAILS.C.0010.check.0001
    message: "Negative constraint without positive alternative"
    severity: WARNING
    languages: [generic]
    patterns:
      - pattern-regex: "(?i)\\b(never|don't|do not|avoid)\\b"
      - pattern-not-regex: "(?i)\\b(never|don't|do not|avoid)\\b[^\\n]*([—–-]|instead|use|prefer|rather)"
    paths:
      include:
        - "{{instruction_files}}"
