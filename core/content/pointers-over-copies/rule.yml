rules:
  - id: RRAILS.C.0011.check.0001
    message: "Large code block embedded in instruction file — use file:line pointers"
    severity: WARNING
    languages: [generic]
    pattern-regex: "```[a-z]*\\n(.*\\n){10,}```"
    paths:
      include:
        - "{{instruction_files}}"
