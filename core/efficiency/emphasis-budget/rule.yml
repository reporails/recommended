rules:
- id: RRAILS.E.0002.emphasis-overuse
  message: Emphasis overuse
  severity: WARNING
  languages:
  - generic
  paths:
    include:
    - '{{instruction_files}}'
  pattern-regex: 
    (?si)(?:IMPORTANT|CRITICAL|MUST|NEVER)[\s\S]*?(?:IMPORTANT|CRITICAL|MUST|NEVER)[\s\S]*?(?:IMPORTANT|CRITICAL|MUST|NEVER)[\s\S]*?(?:IMPORTANT|CRITICAL|MUST|NEVER)
